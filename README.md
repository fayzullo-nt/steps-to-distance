Step Distance Calculator

A simple Python program that converts the number of steps into meters.
Useful as a beginner-friendly exercise in variables, functions, and basic input/output.

How it works

The user enters the number of steps.

The program multiplies the steps by an average step length (0.8 meters).

It prints the total distance walked.
